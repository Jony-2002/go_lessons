//?  Timer
// var expired string
// select {
// case <-time.After(time.Duration(timerDuration) * time.Second):
// 	expired = "expired"
// }

// return expired
//?  Timer
// ! ============================== Func EXE : Level 3 =======================
// !==========================  One
func DevideByOne(portal nastroyki) {
	//* 1 ar nastroyki thet "numberA" kak int ared dethd
	var devideBy = numberA / 2
	//* 2 print "devideBy"
	//* 3 dam func DevideByOne kiwet ar main khy ar portal 4 pitet
}

// ! =====================  Two
func JoinMyName(portal nastroyki) {
	//* 1 ar portal nastroyki piridavatet "Name" kak string "Surname" kak string
	//* 2 ar JoinMyName Body kinet print "Name" at "Surname"
	//* 3 dam func JoinMyName ar main qiwet khu khu nomaat famileya kak string "" ar portal pitet
}

// ! =====================  Three
func whoIs(portal nastroyki) {
	//* 1 ar portal nastroyki piridavatet "Name" kak string
	// ? If am ca kheyd abor khu bayoth vethet (if else)
	//* 2 if qate chiset ( if "Name" == "John" ) id kikht print "You are John"
	//* else print you are ot John
}

